#####################################################################################################
######################## Template velocity de satelitesVisible ######################################
#####################################################################################################
#set( $dateFormat = "yyyy-MM-dd'T'HH:mm:ss.SSS'Z'" )
{
"satellitesVisibleCount":[
     #foreach($sateliteTimeCoordinate in $satelitesVisible)
        {
        "epochHeader":"$localDateTimeTool.format($sateliteTimeCoordinate.epochHeaderRecord, $dateFormat)",
        "satelliteCount": $sateliteTimeCoordinate.satelites.size()
        } #if($foreach.hasNext ),#end
    #end
]
}
