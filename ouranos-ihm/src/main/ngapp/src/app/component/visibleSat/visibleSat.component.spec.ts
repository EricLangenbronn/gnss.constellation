import { TestBed, async } from '@angular/core/testing';

import { VisibleSatComponent } from './visibleSat.component';

describe('AppComponent', () => {
  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [
        VisibleSatComponent
      ],
    }).compileComponents();
  }));


});
